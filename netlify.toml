[[redirects]] 
    from = "/*"
    to = "/index.html"
    status = 200

[build]
  command = "bun run build"
  publish = "dist"  # Vite outputs to the "dist" folder

[build.environment]
  NODE_VERSION = "18" # Use Node.js for compatibility
  BUN_VERSION = "latest"